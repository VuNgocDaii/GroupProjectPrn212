<Page x:Class="GroupProjectPrn212.Views.HocVienView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Học viên"
      Background="#F3F4F6">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Bộ lọc" Style="{StaticResource GroupTitleStyle}"/>
                    <WrapPanel>
                        <TextBox x:Name="txtKeyword" Width="280" Style="{StaticResource InputTextBoxStyle}"/><Button Content="Tìm kiếm" Click="btnSearch_Click" Style="{StaticResource PrimaryButtonStyle}" Width="120"/><Button Content="Làm mới" Click="btnRefresh_Click" Style="{StaticResource SecondaryButtonStyle}" Width="120"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Thông tin học viên" Style="{StaticResource GroupTitleStyle}"/>

<Grid>
    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
    <StackPanel Grid.Column="0" Margin="0,0,12,0">
        <TextBlock Text="Mã học viên" Style="{StaticResource FieldLabelStyle}"/>
        <TextBox x:Name="txtMaHocVien" Style="{StaticResource InputTextBoxStyle}"/>
        <TextBlock Text="Ngày sinh" Style="{StaticResource FieldLabelStyle}"/>
        <DatePicker x:Name="dpNgaySinh" Style="{StaticResource InputDatePickerStyle}"/>
        <TextBlock Text="Số điện thoại" Style="{StaticResource FieldLabelStyle}"/>
        <TextBox x:Name="txtSoDienThoai" Style="{StaticResource InputTextBoxStyle}"/>
        <TextBlock Text="Địa chỉ" Style="{StaticResource FieldLabelStyle}"/>
        <TextBox x:Name="txtDiaChi" Style="{StaticResource InputTextBoxStyle}"/>
    </StackPanel>
    <StackPanel Grid.Column="1">
        <TextBlock Text="Họ tên" Style="{StaticResource FieldLabelStyle}"/>
        <TextBox x:Name="txtHoTen" Style="{StaticResource InputTextBoxStyle}"/>
        <TextBlock Text="Giới tính" Style="{StaticResource FieldLabelStyle}"/>
        <ComboBox x:Name="cbGioiTinh" Style="{StaticResource InputComboBoxStyle}">
            <ComboBoxItem Content="Nam"/>
            <ComboBoxItem Content="Nữ"/>
        </ComboBox>
        <TextBlock Text="Email" Style="{StaticResource FieldLabelStyle}"/>
        <TextBox x:Name="txtEmail" Style="{StaticResource InputTextBoxStyle}"/>
    </StackPanel>
</Grid>

                    <WrapPanel HorizontalAlignment="Right" Margin="0,8,0,0">
                        <Button x:Name="btnAdd" Content="Thêm" Click="btnAdd_Click" Style="{StaticResource PrimaryButtonStyle}" Width="110"/>
                        <Button x:Name="btnUpdate" Content="Sửa" Click="btnUpdate_Click" Style="{StaticResource SecondaryButtonStyle}" Width="110"/>
                        <Button x:Name="btnDelete" Content="Xóa" Click="btnDelete_Click" Style="{StaticResource DangerButtonStyle}" Width="110"/>
                        <Button x:Name="btnClear" Content="Clear" Click="btnClear_Click" Style="{StaticResource SecondaryButtonStyle}" Width="110"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Danh sách học viên" Style="{StaticResource GroupTitleStyle}"/>
                    <DataGrid x:Name="dgHocVien" SelectionChanged="dgHocVien_SelectionChanged">
                        <DataGrid.Columns>

<DataGridTextColumn Header="ID" Binding="{Binding HocVienId}" Width="70"/>
<DataGridTextColumn Header="Mã" Binding="{Binding MaHocVien}" Width="120"/>
<DataGridTextColumn Header="Họ tên" Binding="{Binding HoTen}" Width="*"/>
<DataGridTextColumn Header="Ngày sinh" Binding="{Binding NgaySinh}" Width="120"/>
<DataGridTextColumn Header="Giới tính" Binding="{Binding GioiTinh}" Width="90"/>
<DataGridTextColumn Header="SĐT" Binding="{Binding SoDienThoai}" Width="130"/>
<DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>

                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
